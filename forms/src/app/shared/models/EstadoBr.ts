export interface EstadoBr {
  id: number,
	sigla: string,
	nome: string
}

//interface -> somente assinaturas do que precisa ter no objeto que ira implementar essa interface, não tem implementação ou corpo!
